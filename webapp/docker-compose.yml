networks:
  serving_prod_net:
    external: true

services:
  webapp:
    container_name: webapp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    volumes:
      - ../data:/data
      - ../artifacts:/artifacts
    networks:
      - serving_prod_net
